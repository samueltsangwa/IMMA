<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.imma">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Confirm_password"
            android:label="Reset password">
        </activity>
        <activity android:name=".Forget_password"
            android:label="Forget password"/>
        <activity android:name=".NewsInDetails" />
        <activity
            android:name=".Breaking_News"
            android:label="Breaking news" />
        <activity
            android:name=".Manual"
            android:label="User Manual" />
        <activity
            android:name=".Reset"
            android:label="Reset password" />
        <activity
            android:name=".Dashboard"
            android:label="Dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Register"
            android:label="Register" />
        <activity
            android:name=".Login"
            android:label="Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>